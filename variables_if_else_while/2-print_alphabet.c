#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
* main- This cause the alphabet to be displayed in lowercase. *
*
* Return: Always (Success)
*/

int main(void)
{
	char alphabet;
		for (alphabet = 'a'; alphabet <= 'z' ; alphabet++)
	{ putchar(alphabet);
		}
	{ putchar('\n');
		}
	return (0);
}
