#include "main.h"
/**
 *  _abs - Takes an integer and returns absolute value.
 * @num: variable
 *
 * Return:0
 */

int _abs(int num)
{
	if (num < 0)
	{
		return (-num);
	}
	else
	{
		return (num);
	}
		return (0);
}
