#include "main.h"
#include <stdio.h>
/**
 * add - takes two integers adds them together, and returns the result.
 * @a: variable
 * @b: result
 *
 * Return: void
 */

int add(int a, int b)
{
	int result = a + b;

	return (result);
}
